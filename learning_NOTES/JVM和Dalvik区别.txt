①java虚拟机运行的是java字节码，而Dalvik虚拟机运行的则是其专有的文件格式DEX。

②JVM基于栈，基于栈的机器必须使用指令来载入和操作栈上的数据，所需指令更多；Dalvik则是基于寄存器的

③每个Dalvik应用程序都是一个独立的进程


安卓应用的编译及运行流程：

java文件 ---通过JDK--->  class文件  ---通过Dx工具--->  Dex文件  ---优化--->  apk文件(清单文件和资源文件通过aapt工具打包进apk)  ------> 手机